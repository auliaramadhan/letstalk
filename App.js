import React from 'react';
import AppContainer from './src/config/router'
import { Root } from 'native-base';

const App = () => {
  return (
    <Root>
      <AppContainer />
    </Root>
  );
};

export default App;
